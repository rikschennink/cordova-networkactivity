# PhoneGap NetworkActivity Plugin

Toggle network activity indicator on iOS.

JS file is based on RequireJS module pattern.

```javascript
NetworkActivity.activityStart();
NetworkActivity.activityStop();
```